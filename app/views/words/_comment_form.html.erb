<% form_tag :action => 'add_comment' %>
  <%= text_field 'comment', 'title' %>
  <%= text_field 'comment', 'comment' %>

  <%= submit_tag 'Add Comment' %>

  <%= hidden_field 'commentable', 'commentable', :value => @definition_type %>
  <%= hidden_field 'commentable', 'commentable_id', :value => @definition_id %>
<% form_tag %>
