<span class="votes" id="votes-definition-<%=h definition.id %>"><%= definition.percent_liked %>% liked, by
  <% @user = User.find(definition.user_id) unless definition.user_id.nil?%>
  <%=link_to(@user.username, @user) + " on " + definition.updated_at.strftime("%B %d, %Y") + "." %>
</span>